openapi: 3.0.2
info:
  version: 1.0.0
  title: Fixture - Discriminator
tags:
  - name: pet
paths:
  /all_of_terrier:
    post:
      tags:
        - pet
      operationId: all_of_terrier
      responses:
        '200':
          description: Successful operation
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Terrier'
components:
  schemas:
    Pet:
      type: object
      required:
        - id
        - type
      properties:
        id:
          type: integer
        type:
          type: string
    Dog:
      type: object
      required:
        - breed
      properties:
        breed:
          type: string
    Terrier:
      allOf:
        - $ref: '#/components/schemas/Pet'
        - $ref: '#/components/schemas/Dog'
        -
          type: object
          required:
            - group
          properties:
            group:
              type: string
              default: hunting
